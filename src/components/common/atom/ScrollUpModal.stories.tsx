import type { StoryObj } from '@storybook/react';
import ScrollUpModal from './ScrollUpModal';

export default {
  title: 'ScrollUpModal',
  component: ScrollUpModal,
};
type Story = StoryObj<typeof ScrollUpModal>;
export const Default: Story = {};
